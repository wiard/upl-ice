[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "airmarket"
version = "0.1.0"
description = "A small local warrant-gated marketplace for avatar work."
requires-python = ">=3.10"

